version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    image: wordguesser:ci

    ports:
      - "9292:9292"

    volumes:
      - .:/app

    environment:
      - PORT=9292
      - RACK_ENV=development
      - RAILS_ENV=development